<h1><%= @attraction.name %></h1>

<%= button_to "Go on this ride", ride_path, method: :get, params: {"attraction_id" => @attraction.id, "user_id" => User.find(session[:user_id])} %>
<% if @user.admin == true %>
    <%= link_to 'Edit Attraction', edit_attraction_path(@attraction) %>
<% end %>